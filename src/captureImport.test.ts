import{describe,expect,it}from'vitest';import{calculateFinance}from'./finance';import{importedAssumptions,importedProducts}from'./captureImport';import{assumptions}from'./fixtures/demo';import type{ExtensionCapture}from'./types';
const capture=(bid?:number,bidText?:string):ExtensionCapture=>({captureId:'capture-1',sourceUrl:'https://www.ebth.com/users/followed_items',pageTitle:'Followed Items',capturedAt:'2026-07-29T00:00:00Z',status:'success',width:100,height:100,devicePixelRatio:1,warnings:[],listings:[{id:'144001',sourcePageUrl:'https://www.ebth.com/users/followed_items',title:'Quartz Hololith Bangle',currentBidCents:bid,bidText,extractionConfidence:100,extractionWarnings:[]}]});
describe('extension capture handoff',()=>{it('survives payload/token storage serialization and web import',()=>{const stored=JSON.parse(JSON.stringify({capture_token:capture(2000,'Current Bid $20')})).capture_token as ExtensionCapture,product=importedProducts(stored)[0],a=importedAssumptions(product,assumptions(0));expect(product).toMatchObject({currentBidCents:2000,bidText:'Current Bid $20'});expect(a.currentBidCents).toBe(2000);expect(calculateFinance(a).acquisitionCents).toBeGreaterThan(2000)});it('preserves a $45 bid as 4500 cents for the Current bid ($) field',()=>{const product=importedProducts(capture(4500,'$45'))[0],a=importedAssumptions(product,assumptions(0));expect(product.currentBidCents).toBe(4500);expect(a.currentBidCents!/100).toBe(45)});it('does not normalize a missing captured bid to zero',()=>{const product=importedProducts(capture())[0];expect(importedAssumptions(product,assumptions(0)).currentBidCents).toBeUndefined()})});
