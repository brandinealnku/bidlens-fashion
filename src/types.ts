export type Box={x:number;y:number;width:number;height:number};
export type PageCaptureResult={sourceUrl:string;pageTitle:string;capturedAt:string;width:number;height:number;status:'success'|'partial'|'failed';warnings:string[];screenshotUrl:string;html?:string};
export type DetectedProduct={id:string;sourcePageUrl:string;listingUrl?:string;title?:string;currentBidCents?:number;bidText?:string;timeRemaining?:string;imageUrl?:string;croppedImagePath?:string;screenshotBoundingBox?:Box;extractionConfidence:number;extractionWarnings:string[];excluded?:boolean;userConfirmed?:boolean;researchSelected?:boolean;monitoringSelected?:boolean;monitoringStatus?:'not_selected'|'selected'|'paused';lastObservedBidCents?:number;lastObservedAt?:string;auctionEndIso?:string;maximumBidCents?:number};
export type ExtensionCapture={captureId:string;sourceUrl:string;pageTitle:string;capturedAt:string;status:'success'|'partial'|'failed';width:number;height:number;devicePixelRatio:number;screenshotDataUrl?:string;screenshotParts?:{index:number;dataUrl:string;width:number;height:number}[];warnings:string[];listings:DetectedProduct[];sessionId?:string};
export type ProductIdentification={productId:string;proposedBrand?:string;proposedCategory?:string;proposedProductType?:string;proposedModel?:string;proposedMaterial?:string;proposedCondition?:string;suggestedSearchQuery:string;identificationConfidence:number;identificationNotes:string[];userConfirmed:boolean};
export type ComparableSale={id:string;productId:string;sourceName:string;sourceUrl?:string;title:string;priceCents:number;shippingCents?:number;status:'sold'|'completed'|'active'|'retail'|'mock';matchConfidence:number;notes?:string};
export type Assumptions={currentBidCents:number;buyerPremiumBps:number;salesTaxBps:number;inboundShippingCents:number;otherAcquisitionCents:number;expectedResaleCents:number;marketplaceFeeBps:number;processingFeeBps:number;outboundShippingCents:number;packagingCents:number;desiredProfitCents:number;desiredRoiBps:number;maxBidMode:'profit'|'roi'};
export type FinanceResult={buyerPremiumCents:number;salesTaxCents:number;acquisitionCents:number;marketplaceFeeCents:number;processingFeeCents:number;netProceedsCents:number;profitCents:number;roiBps:number|null;breakEvenResaleCents:number|null;maximumBidCents:number|null};
export type Analysis={product:DetectedProduct;identification:ProductIdentification;comparables:ComparableSale[];estimates:{conservative:number;expected:number;optimistic:number;confidence:number};assumptions:Assumptions;finance:FinanceResult;score:number;label:string;reasons:string[];warnings:string[]};
