export type Box={x:number;y:number;width:number;height:number};
export type PageCaptureResult={sourceUrl:string;pageTitle:string;capturedAt:string;width:number;height:number;status:'success'|'partial'|'failed';warnings:string[];screenshotUrl:string;html?:string};
export type DetectedProduct={id:string;sourcePageUrl:string;listingUrl?:string;title?:string;currentBidCents?:number;bidText?:string;timeRemaining?:string;imageUrl?:string;croppedImagePath?:string;screenshotBoundingBox?:Box;extractionConfidence:number;extractionWarnings:string[];excluded?:boolean;userConfirmed?:boolean;researchSelected?:boolean;monitoringSelected?:boolean;monitoringStatus?:'not_selected'|'selected'|'paused';lastObservedBidCents?:number;lastObservedAt?:string;auctionEndIso?:string;maximumBidCents?:number};
export type ExtensionCapture={captureId:string;sourceUrl:string;pageTitle:string;capturedAt:string;status:'success'|'partial'|'failed';width:number;height:number;devicePixelRatio:number;screenshotDataUrl?:string;screenshotParts?:{index:number;dataUrl:string;width:number;height:number}[];warnings:string[];listings:DetectedProduct[];sessionId?:string};
export type ProductIdentification={productId:string;proposedBrand?:string;proposedCategory?:string;proposedProductType?:string;proposedModel?:string;proposedMaterial?:string;proposedCondition?:string;suggestedSearchQuery:string;identificationConfidence:number;identificationNotes:string[];userConfirmed:boolean};
export type ComparableSale={id:string;productId:string;sourceName:string;sourceUrl?:string;title:string;priceCents:number;shippingCents?:number;status:'sold'|'completed'|'active'|'retail'|'mock';matchConfidence:number;notes?:string};
export type ComparableProviderName='ebay-browse'|'manual'|'demo'|'future-sold-provider';
export type EvidenceType='active-listing'|'sold-listing'|'retail-reference'|'user-entered';
export type EvidenceStatus='pending'|'approved'|'rejected';
export type ComparableEvidence={id:string;productId:string;provider:ComparableProviderName;evidenceType:EvidenceType;title:string;url?:string;imageUrl?:string;itemPriceCents:number;shippingCents?:number;totalPriceCents:number;brand?:string;category?:string;condition?:string;material?:string;model?:string;matchScore:number;matchReasons:string[];mismatchWarnings:string[];status:EvidenceStatus;sourceTimestamp?:string;sourceMarketplace?:string;soldDate?:string;notes?:string;imageSearchMatch?:boolean};
export type ResearchableProduct={id:string;title:string;brand?:string;category?:string;productType?:string;model?:string;material?:string;color?:string;condition?:string;imageUrl?:string};
export type ComparableSearchOptions={imageSearch?:boolean;limit?:number};
export type ComparableSearchResult={productId:string;queriesUsed:string[];results:ComparableEvidence[];warnings:string[];provider:ComparableProviderName;searchedAt:string};
export interface ComparableProvider{search(product:ResearchableProduct,options?:ComparableSearchOptions):Promise<ComparableSearchResult>}
export type ResearchStatus='Not researched'|'Searching'|'Results ready'|'Needs review'|'Evidence approved'|'Insufficient evidence'|'Provider unavailable'|'User estimate locked';
export type RealizationFactors={conservative:number;expected:number;optimistic:number};
export type EvidenceValuation={productId:string;conservativeCents:number;expectedCents:number;optimisticCents:number;confidence:number;activeComparableCount:number;soldComparableCount:number;evidenceCount:number;medianCents:number|null;weightedCentralCents:number|null;priceRangeCents:[number,number]|null;outlierWarnings:string[];excludedEvidenceIds:string[];source:'evidence'|'insufficient-evidence'};
export type ResaleEstimate={productId:string;conservativeCents:number;expectedCents:number;optimisticCents:number;estimateSource:'comparables'|'category-model'|'title-heuristic'|'user-entered'|'placeholder';confidence:number;rationale:string[];comparableCount:number};
export type Assumptions={currentBidCents:number|undefined;buyerPremiumBps:number;salesTaxBps:number;inboundShippingCents:number;otherAcquisitionCents:number;expectedResaleCents:number;conservativeResaleCents?:number;optimisticResaleCents?:number;resaleLocked?:boolean;marketplaceFeeBps:number;processingFeeBps:number;outboundShippingCents:number;packagingCents:number;desiredProfitCents:number;desiredRoiBps:number;maxBidMode:'profit'|'roi'};
export type FinanceResult={buyerPremiumCents:number;salesTaxCents:number;acquisitionCents:number;marketplaceFeeCents:number;processingFeeCents:number;netProceedsCents:number;profitCents:number;roiBps:number|null;breakEvenResaleCents:number|null;maximumBidCents:number|null};
export type Analysis={product:DetectedProduct;identification:ProductIdentification;comparables:ComparableSale[];estimates:{conservative:number;expected:number;optimistic:number;confidence:number};assumptions:Assumptions;finance:FinanceResult;score:number;label:string;reasons:string[];warnings:string[]};
