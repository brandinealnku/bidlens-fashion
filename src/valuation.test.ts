import{describe,expect,it}from'vitest';import{estimateResale,preserveLockedEstimate}from'./valuation';import type{DetectedProduct,ProductIdentification}from'./types';
const product=(title:string,bid=1000):DetectedProduct=>({id:title,sourcePageUrl:'fixture',title,currentBidCents:bid,extractionConfidence:80,extractionWarnings:[]});
const id=(brand='',condition='Good',category='Accessories'):ProductIdentification=>({productId:'x',proposedBrand:brand,proposedCategory:category,proposedCondition:condition,suggestedSearchQuery:'',identificationConfidence:50,identificationNotes:[],userConfirmed:false});
describe('product-specific valuation',()=>{it('is deterministic and category-specific',()=>{expect(estimateResale(product('Leather handbag'),id('Coach'))).toEqual(estimateResale(product('Leather handbag'),id('Coach')));expect(estimateResale(product('Leather handbag'),id('Coach')).expectedCents).not.toBe(estimateResale(product('Silk scarf'),id('','Good','Scarf')).expectedCents)});it('adjusts brand and condition',()=>{expect(estimateResale(product('Bracelet'),id('Gucci')).expectedCents).toBeGreaterThan(estimateResale(product('Bracelet'),id()).expectedCents);expect(estimateResale(product('Coat'),id('','Damaged','Coat')).expectedCents).toBeLessThan(estimateResale(product('Coat'),id('','Excellent','Coat')).expectedCents)});it('uses a low-confidence heuristic without comparables and does not copy bid',()=>{const e=estimateResale(product('Vintage unknown',9900),id());expect(e.comparableCount).toBe(0);expect(e.confidence).toBeLessThan(70);expect(e.expectedCents).not.toBe(9900)});it('preserves a locked user estimate',()=>{const e=estimateResale(product('Bag'),id());expect(preserveLockedEstimate(e,true,{conservativeCents:1,expectedCents:2,optimisticCents:3})).toMatchObject({expectedCents:2,estimateSource:'user-entered'})})});
