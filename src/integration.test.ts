import{it,expect}from'vitest';import{capture,products,identifications,comparables,assumptions}from'./fixtures/demo';import{calculateFinance}from'./finance';import{scoreAnalysis,rankAnalyses}from'./ranking';
it('runs demo capture → extraction → review → assumptions → ranking',()=>{expect(capture.status).toBe('success');expect(products).toHaveLength(6);const results=products.map((product,i)=>{const identification={...identifications[i],userConfirmed:true},cs=comparables.filter(c=>c.productId===product.id),a=assumptions(i),base={product,identification,comparables:cs,estimates:{conservative:cs[0].priceCents,expected:a.expectedResaleCents,optimistic:cs[2].priceCents,confidence:72},assumptions:a,finance:calculateFinance(a),warnings:[]};return{...base,...scoreAnalysis(base)}});expect(rankAnalyses(results)).toHaveLength(6);expect(rankAnalyses(results)[0].score).toBeGreaterThan(0)});
