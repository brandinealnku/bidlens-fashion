import{describe,expect,it}from'vitest';import{approved,calculate,defaultAssumptions,median,resale,type Comparable,type Item}from'./mobile';
const comp=(priceCents:number,matchQuality:Comparable['matchQuality']='strong',evidenceType:Comparable['evidenceType']='active'):Comparable=>({id:String(priceCents),priceCents,shippingCents:0,marketplace:'manual',evidenceType,condition:'good',matchQuality});
const item=(comparables:Comparable[],bid=1000):Item=>({id:'test',name:'Bag',currentBidCents:bid,shippingCents:0,premiumBps:0,taxBps:0,condition:'good',notes:'',otherAcquisitionCents:0,comparables,assumptions:{...defaultAssumptions,outboundShippingCents:0,packagingCents:0},updatedAt:'2026-01-01'});
describe('mobile valuation',()=>{it('calculates an integer-cent median',()=>expect(median([300,100,200])).toBe(200));it('discounts active listings by scenario',()=>expect(resale(comp(10000),defaultAssumptions,'expected')).toBe(7500));it('does not discount user-observed sold prices',()=>expect(resale(comp(10000,'exact','sold'),defaultAssumptions,'expected')).toBe(10000));it('adjusts retail references',()=>expect(resale(comp(10000,'exact','retail'),defaultAssumptions,'expected')).toBe(5000));it('excludes weak and opt-in similar evidence',()=>{expect(approved(comp(100,'weak'))).toBe(false);expect(approved(comp(100,'similar'))).toBe(false)});it('returns PASS without evidence',()=>expect(calculate(item([])).decision).toBe('PASS'));it('warns with one credible comparable',()=>expect(calculate(item([comp(10000)],1000)).decision).toBe('MAYBE'));it('returns BUY with evidence and sufficient room',()=>expect(calculate(item([comp(10000),comp(11000)],1000)).decision).toBe('BUY'));it('returns PASS above the maximum bid',()=>expect(calculate(item([comp(10000),comp(11000)],100000)).decision).toBe('PASS'));});
describe('mobile-first accessibility contract',()=>{it('keeps IndexedDB available without research providers',()=>expect(typeof defaultAssumptions.marketplaceFeeBps).toBe('number'))});
