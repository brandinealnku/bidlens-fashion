import{describe,expect,it}from'vitest';import{scrollPositions,segmentGroups}from'./capture';describe('capture calculations',()=>{it('caps positions and overlaps',()=>{const p=scrollPositions(100000,800,80,80);expect(p.length).toBe(80);expect(p[1]).toBe(720)});it('handles empty and tall pages',()=>{expect(scrollPositions(0,800)).toEqual([]);expect(segmentGroups(65000)).toEqual([{start:0,height:30000},{start:30000,height:30000},{start:60000,height:5000}])})});
